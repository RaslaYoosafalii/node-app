
<link rel="stylesheet" href="/styles/userHome.css">
  <div class="home-container">
    <div class="home-box">
      <h1 class="welcome-title">👋 Welcome to Your homepage,  {{email}}!</h1>
       
      <p class="login-info">{{{message}}}, You are now logged in!</p>
     
      <a href="/user/logout"><button class="logout-btn">Logout</button></a>
    </div>
  </div>
<script>
  window.addEventListener("pageshow", function (event) {
    if (event.persisted || window.performance.getEntriesByType("navigation")[0].type === "back_forward") {
      window.location.reload(); // Force reload if accessed via back button
    }
  });
</script>
